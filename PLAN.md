# Fediblog — UI & UX Plan

**Last Updated:** Aug 18, 2026
**Status:** Backend federation, API, and scalability infrastructure is **feature-complete** (Phases 1–45, 1021/1021 tests passing). This plan is **UI-first**: raise the WebUI to a polished, fast, responsive, accessible microblog. New backend work is deferred until a UI feature specifically requires it.

**WebUI:** `src/ActivityPub.WebUI/` — Razor MVC, SignalR, vanilla JS (`wwwroot/js/`: `compose.js`, `menu.js`, `toast.js`), single `site.css` with `:root` design tokens + `[data-theme=dark]` overrides.

---

## UI Testing (brief)

- Browser/UI tests run in **delegated Playwright subagents** (not inline), after each meaningful change batch, before marking a feature complete.
- **Launch:** `docker compose -f src/ActivityPub.WebUI/docker-compose.yml up -d --build`; wait for HTTP 200 at `http://localhost:8080`.
- Exercise changed flows (auth, compose, timeline, interactions, profiles, admin); verify elements, text, behavior; report pass/fail with screenshots + console errors.
- **Localhost:** no real federation — mock remote rows directly in the DB (SQLite in the `fediblog-data` volume) or POST crafted ActivityPub payloads to the local inbox.
- **Integration host:** `https://openpub.luit.ink/` (real federation; test target **@RayvenMX@mastodon.world**).
- **Cleanup:** `docker compose -f src/ActivityPub.WebUI/docker-compose.yml down` (add `-v` to discard DB fixtures).

---

## Phases

### Phase 46: UI Performance & Perceived Speed
Make the timeline and interactions feel instant.
1. ✅ Client-side "load more" + IntersectionObserver infinite scroll on Timeline **and** Search (`?page=` cursor, `data-next`, de-dupe by `data-activity-id`, skeleton loader, cursor hidden on the last page, `&amp;`-decoded cursor URLs).
2. ✅ Defer non-critical note cards; audit `fetchpriority`/`loading="lazy"` on images — every `<img>` now has an explicit loading/fetchpriority strategy + dimensions; note images reserve a stable box (aspect-ratio + min-height) so lazy loads cause no layout shift.
3. ✅ `prefers-reduced-motion: reduce` block in `site.css` zeroes animation/transition durations and scroll-behavior.
 4. ✅ Measured LCP/TTI on home timeline via delegated Playwright subagent. Empty-timeline baseline: LCP 32 ms, FCP 32 ms, load 20 ms, total JS payload ≈ 29 KB (app.js 26 KB + menu.js 2 KB + theme.js 0.4 KB), CSS 300 B. All assets served from cache; no images/fonts/third-party resources. Populated-timeline re-measure deferred until Phase 47 (responsive pass adds real content).
 5. ✅ Consolidate all JS into a single `window.FB` module loader (`app.js`) + feature modules (`menu.js`, `compose.js`, `poll.js`, `search.js`, `suggestions.js`); `theme.js` bootstraps dark mode pre-SSR; SignalR CDN script deduped in the layout.

### Phase 47: Responsive & Mobile-First
1. ✅ Audited pages at 320 / 768 / 1024 / 1440px via delegated Playwright; fixed horizontal overflow (none at any width) and raised touch-targets to ≥44px (`.btn`, `.btn-action`, `.btn-more`, `.btn-unfollow`, `.nav-hamburger`, `.theme-toggle`, `.nav-group-toggle`, `.search-tab`, `.filter-tab`, `.cw-toggle-btn`, `.note-more-item`). Also fixed a pre-existing malformed `site.css` where the `@media (max-width: 768px)` block was missing its opening brace, leaving `.form-actions`/`.hero-actions`/`.error-*`/`.profile-banner`/`.profile-avatar` un-scoped.
2. ✅ Sticky compose FAB (`.compose-fab`, shown ≤768px, hides the in-header compose button) + mobile bottom nav (`.mobile-bottom-nav`, Home/Search/Inbox/Profile) shown ≤768px, hidden ≤480px where the drawer suffices; `body` gains bottom padding so content clears the fixed nav.
3. ✅ Mobile drawer + scrim now keyboard- and screen-reader-operable: `Esc` closes, `aria-hidden` toggles, focus restored to trigger on close, Tab focus-trap, click-outside close. `menu.js` moved to `_Layout` (deduped from 4 views) and `app.js` now loads **before** `menu.js`/view scripts so `window.FB` exists (fixes pre-existing `ReferenceError: FB is not defined` on every module script).
4. ✅ Media queries for note cards, poll bars, admin tables (`.admin-table` horizontal-scroll wrapper as last resort), attachment grids, font-size floors.

### Phase 48: Interaction & Real-Time UX
1. ✅ Live timeline refresh via SignalR (new notes prepend without reload) + SSE fallback.
2. ✅ Notifications: real-time badge + unread counts (server-seeded via `/notifications/badge`), mark-as-read (persisted unread cursor in `UserPreferences`), relative timestamps, deep links to source note (`/timeline?note=<id>` → scroll + flash). Likes/boosts/replies now address the target author in their `to`, so they land in the recipient's inbox; `NewNotification` SignalR event emitted on like/boost/reply/follow.
3. ✅ Optimistic UI everywhere safe, with rollback on failure; replace form-submit reloads with `fetch` + DOM patch. Like/boost now reconcile with the server-rendered card fragment (`/timeline/card/<id>`) after a mutation (single delegated handler, safe for live-inserted cards). Follow/unfollow (profile) and community join/leave are optimistic toggles with rollback + server reconciliation (`/Profile/State`, `/communities/show`). Fixed `ExtractNote` to rehydrate the `Object` property from stored `JsonData` (JsonElement), so boosted/rehydrated notes render with correct interaction counts; fixed `GetFollowerCountAsync` to exclude `Undo` activities so the follower count drops after an unfollow.
4. ✅ Command palette / global search — Ctrl+K / ⌘K overlay (or the header "⌘ Ctrl K" trigger) fuzzy-matches notes, people, hashtags, and communities in one box with keyboard nav (↑/↓ + Enter + Esc). Results come from a new compact `GET /search/json?q=` endpoint and are fuzzy-scored client-side (ordered-subsequence match with position/recency + substring bonuses); groups render capped at 6 each with match highlighting. Selecting navigates to the deep link (note → `/timeline?note=<id>`, user → `/Profile?username=<u>`, community → `/communities/show?communityId=<id>`, hashtag → `/search?tab=hashtags`); with no match, Enter opens the full search page. New `wwwroot/js/palette.js` (FB module, loaded globally for authenticated users) + overlay markup in `_Layout.cshtml` + `.palette-*` styles.

### Phase 49: Design System & Visual Consistency
1. ✅ Extract component kit from `site.css` (`.note-card`, `.btn*`, `.admin-card`, `.stat-card`, `.avatar-*`, `.empty-state`, `.page-header`); remove ad-hoc inline `<style>`/`style=""`. Added a **Component Kit** section to `site.css` (after `.inline-form`) holding reusable primitives, all token-driven (`var(--…)`) so they theme in light + dark. (a) Removed the two per-page inline `<style>` blocks — `Suggestions/Index.cshtml` (Discover: `.discover-container`, `.suggestions-grid`, `.suggestion-card/-avatar/-info`, `.display-name`, `.username`, `.bio`, `.follower-count`, `.suggestion-actions`, `.btn-follow/-muted/-add-filter/-remove-filter`, `.filter-list`, `.filter-keyword`, `.add-filter-form`, `#filterInput`) and `FederationHealth/Index.cshtml` (`.health-status-banner`, `.status-healthy/-degraded/-warning/-critical`, `.last-checked`, `.health-grid`, `.health-card`, `.health-table`, `.probe-form`, `.probe-input-row`, `.badge-status`) — hard-coded colors (`#1d9bf0`, `#eee`, `#333`, `#fff`, `#d4edda`…) replaced with semantic tokens (`--color-info`, `--color-border(-light)`, `--color-surface`, `--color-text(-secondary/-muted)`, `--color-danger`, `--color-success-soft`, `--color-accent-soft`, `--space-*`, `--radius-md`, `--shadow-card`); the health status-banner tones now use the soft semantic backgrounds so they theme in dark mode. (b) Replaced all 13 static inline `style=""` attrs with kit classes, consolidating the 4 repeated patterns: the identical skeleton header row (Timeline + Search `display:flex;gap:0.5rem;…`) → `.skeleton-row`; its `height:10px` line → `.skeleton-line.thin`; the `_NoteCard` poll `<ul style="list-style:none;margin:0;padding:0">` → `.poll-options-list` (now the class carries the reset); the 6 `style="display:inline"` action forms (_NoteCard poll/like/boost + Admin Users SetAdmin/ToggleBlock + Admin Moderation Delete) → `.inline-form`; the 2 note-more-menu `style="margin:0"` forms → `.no-margin`. Remaining inline styles are only the justified dynamic ones (6× JS `display:none` initial-state toggles + 5× Razor-driven banner `background-image:url('@…')` + 1× Razor-driven poll `width:@…%`). 5 new tests (`ComponentKitTests`: no `<style>` in any view, no static `style=""` in any view, site.css defines the kit primitives, Discover + Federation Health render the kit with no inline style block) — **1132/1132 passing**.
2. 🔧 (in progress) Standardize spacing/typography tokens; no one-off metrics. **Iteration 1 done:** added a **line-height scale** to `:root` (`--lh-none:1`, `--lh-tight:1.2`, `--lh-normal:1.4`, `--lh-body:1.5`, `--lh-relaxed:1.6`) and converted all 16 metric `line-height` literals across `site.css` to tokens (only the deliberate `line-height:0` layout reset remains raw). Tokenized all 58 **exact-duplicate** raw `border-radius` literals that restated an existing token: `4px`→`var(--radius-sm)` (×22), `8px`→`var(--radius-md)` (×27), `6px`→`var(--btn-radius)` (×9). Left the genuinely-distinct radii (`2px/3px/10px/20px/0`, multi-value) as-is — they're not duplicates of existing tokens. 3 new tests (`ComponentKitTests`: line-height tokens defined, no one-off `line-height` literal, no raw radius literal that duplicates a token) — **1135/1135 passing**. **Remaining (next iteration):** convert off-scale `font-size` literals (124 hard-coded, ~43 off-scale: `0.9rem`, `0.8rem`, `1rem`, `1.25rem`, `2rem`, `13px`, …) to the `--font-*` scale (rounding the nearest), and the 428 off-grid/px spacing literals (padding/margin/gap) to the `--space-*` scale (on the 0.25rem grid).
3. ✅ Single inline-SVG icon set (dependency-free), replacing mixed emoji/glyph icons. One shared 28-icon set (reply/like/boost/comment/more/warning/search/caret/home/inbox/profile/prev/next/close/audio/doc/moon/sun/plus/check/redo/bolt/info/cmd/quote/clock/video/link) defined once in `TagHelpers/Icons.cs`+`Icons.Paths.cs` (`Icons.Get(name)` → inline SVG: 24×24 viewBox, stroke-based `fill="none" stroke="currentColor"`, round caps/joins, `aria-hidden`). A Razor tag helper `<icon name="…" class="…"/>` (`TagHelpers/IconTagHelper.cs`) emits `<span class="fb-icon …" aria-hidden="true"><svg…/></span>`; discovered via `@addTagHelper *, ActivityPub.WebUI` in `_ViewImports`. A JS mirror `FB.icon(name)`+`FB.icons` in `app.js` keeps the browser side on the same set (toasts, theme toggle, optimistic like/boost, palette, poll-duration). All views (Layout, NoteCard, Compose, Notifications) and JS (app/palette/poll/toast) now render the set; old emoji/glyph chars (♥ ↻ 💬 ⋮ ♫ 📄 ☀ ☾ ⌕ ▾ ⌂ ✉ 👤 ‹ › ⚠ ✓ ✕ 🔁 ✅ ➕ ⚡ ⌘ ❝ ⏱) removed. CSS `.fb-icon` base makes the inner svg inherit 1em size + currentColor so the set is theme-aware for free. 35 new tests (`IconSetTests`: Icons table well-formed+complete, rendered Layout/Timeline/Notifications carry `fb-icon`/`<svg>` and no stray glyphs, `FB.icon`/`FB.icons` in sync) + updated 8 count-asserting tests to the new icon-then-count structure — 1127/1127 passing.
4. ⬜ Empty states, skeletons, loading affordances on every data-bearing page.
5. ✅ Error + 404/403/500 pages on-brand. `UseStatusCodePagesWithReExecute` now re-runs a new `GET /Home/StatusError?id=<code>` action that reads the original status and renders one shared, status-aware view (`Views/Home/StatusError.cshtml`) — previously *every* non-2xx code (401/400/403/404) was funneled into a hard-coded 404 page and the `?id` param was ignored. Distinct on-brand pages for 404 / 403 (Access Denied) / 410 / 429 / 500 / 502 / 503 / 504 + a generic fallback, each with a dependency-free inline-SVG icon (lock / magnifier / clock / warning-triangle), tone-colored (danger/info/warning) and theme-aware. A direct `GET /Home/Forbidden` route serves the 403 page. `.error-*` CSS modernized onto the design tokens (`--color-*`, `--radius-*`, `--space-*`, `--font-*`) with a circular icon badge + mobile sizing; `Error.cshtml` (500) and `NotFound.cshtml` reuse the same markup. 6 new tests (status re-execution 404, StatusError default/403/503, Forbidden route, unknown-code fallback) — 1021/1021 passing.

### Phase 50: Accessibility (WCAG AA)
1. ✅ Contrast re-audit across light **and** dark themes (all views). Audited every text token + solid-fill button against its actual rendered surface in both themes. **Light:** darkened `--color-accent` (#6c63ff→#574fd6, fixes accent text 4.32→6.01:1 *and* white-on-accent buttons), `--color-danger` (#e74c3c→#d63324, 3.82→4.82:1), `--color-info` (#4a90d9→#3574b5, 3.34→4.86:1), `--color-warning` (#f39c12→#c8760a, 52px icon 2.19→3.47:1 ≥3.0 large-graphic). **Solid-fill white-text buttons** (all failed 2.2–3.0:1) darkened to pass 4.5:1 — `.btn-admin`/`.badge-admin` #4a90d9→#2c6396, `.btn-danger`/`.btn-blockuser` #dc3545→#b83224, `.btn-unblock`/`.badge-member` #27ae60→#1e7e46, `.btn-report` #f39c12→#9a6106 (+AA hover fills). `.char-near` #e6a23c→#9a6106 (2.0→5.14:1). **Dark:** `--dark-accent` #8b84ff→#9a94ff (like/boost active on surface-hover 4.24→4.99:1); added `--dark-danger`/`--dark-info`/`--dark-warning` text overrides (the darker light values failed on dark) + `--dark-*-btn` fills so white-text buttons pass in both themes. Replaced ~35 hard-coded `#6c63ff`/`#e74c3c` with tokens; focus-ring rgba synced to new accent. 4 new contrast tests (light text, dark text, solid buttons, warning/char-near) — 1037/1037 passing.
2. ✅ Focus-visible + logical tab order on all interactive controls (incl. note-more dropdown, poll options). Global `:focus-visible` ring (WCAG 2.4.7) was present but several controls swallowed it with `outline:none` — restored visible rings for `.nav-dropdown-link`, `.search-input`, `.nav-search-input` (each now has a `:focus-visible` box-shadow/outline) and added explicit `.note-more-item:focus-visible` rings; dark-theme overrides included. Note-more dropdown now supports the WAI-ARIA menuitem keyboard pattern: ArrowDown/ArrowUp open + move between items, Home/End jump, Escape closes and returns focus to the toggle, focus moves into the menu on open, Tab is trapped inside. Poll options are display-only (the single Vote button is the only tab stop). No negative `tabindex` on note cards — DOM order equals tab order (WCAG 2.4.3). 6 new a11y tests (1033 total).
3. ✅ Screen-reader pass: `aria-*` on dropdowns/menus/modals (note-more + nav dropdowns carry `aria-controls`/`aria-haspopup="menu"`/`aria-labelledby`), live region for toasts (`role="status"` + `aria-atomic`), `aria-pressed` on like/boost, `aria-label` on icon buttons, labelled poll options, palette `aria-controls`/`aria-expanded` + Tab focus trap. 6 new a11y tests (1027 total).
4. ✅ Keyboard-only walkthrough of the whole app (Home, Timeline, Compose, Search, Trends, Communities, Profile, About, Notifications, command palette, note-more + nav dropdowns) via live browser automation; fixed every trap found. **Bugs fixed:** (a) Note-more & nav-dropdown menus closed *immediately* after opening — the global `document` click handler (bubble phase) re-closed the menu right after the toggle's click handler opened it (even for mouse/Enter); fixed by registering toggle click handlers in the **capture phase** so `stopPropagation` blocks the global handler. (b) Nav dropdowns (Discover/Account) had no menuitem keyboard nav — added ArrowUp/ArrowDown (open + move between items), Home/End (jump), Escape (close + **refocus toggle**), matching the note-more menu pattern. Verified live: tab order logical with no hidden-focusable controls and no traps on every page; palette focus trap + Escape-refocus confirmed. 2 new a11y tests (nav-dropdown arrow nav, capture-phase open) — 1039/1039 passing.

### Phase 51: Rich Media in the UI (leverage existing backend)
 1. ✅ Image lightbox with keyboard nav + prev/next; proper `alt` text. `FB.lightbox` (wwwroot/js/lightbox.js): opens on click **or Enter/Space** (images are `tabindex="0"` `role="button"` with `aria-label`), ArrowLeft/Right + ArrowUp/Down + Home/End navigate (wrapping), Escape closes, Tab focus trapped + restored to the triggering image. `aria-modal` dialog overlay in `_Layout` (z-index 1300, dark scrim, 44px targets, tokens). `TimelineController` now exposes `ImageAttachments` (`NoteImageItem{Url,Alt,Media}`) with real per-image alt (Mastodon `alt` prop, filename fallback). 5 tests.
 2. ✅ Multi-image attachments in a grid. `_NoteCard` renders all Image attachments in a `.note-images.note-images-N` grid (1→full width, 2→2col, 3+→3col) with `data-lightbox-group` so the lightbox cycles every image in the note. Single-image notes hide the prev/next controls.
 3. ✅ Video/audio/document rendering with native players + thumbnails. `TimelineController.ExtractMediaAttachments` → `MediaAttachmentItem{Kind,Url,Name,Media,Preview}` (Video/Audio/Document, kind refined by `mediaType` prefix; url read as string **or** `Link{href}`; optional Mastodon `preview` poster). `_NoteCard` renders `<video controls preload=metadata playsinline poster=...>` + `<source>` + "Download video" fallback; `<audio controls>` + `aria-label` + file name; documents as a download chip. Sensitive/CW media keeps the blurred + `data-cw-content` treatment. **Also fixed:** `ExtractImageUrl` no longer falls back to "first attachment" (a video-only note was mis-rendered as a bogus `<img>`), and `ExtractNote` now uses `SafeDeserializeObject()` so one malformed/foreign activity is skipped instead of 500ing the whole timeline. 6 tests.
4. ✅ Link previews / OEmbed for outbound URLs (client-side, v1). The browser can't reach external hosts (`connect-src`/`img-src 'self'`), so previews are fetched **server-side**. `ILinkPreviewService` (Core) + `LinkPreviewService` (WebUI): SSRF-safe `NormalizeUrl` (absolute http(s), no creds, blocks localhost/loopback/10/8, 172.16/12, 192.168/16, 169.254/16, `::1`, `0.0.0.0`), parses OpenGraph/Twitter/basic `<meta>` + `<title>` (HTML-entity-decoded) with an OEmbed JSON fallback, `IMemoryCache` (6h positive / 10m negative, capped 1MB). `LinkPreviewController`: `GET /linkpreview/card?url=` → JSON card (404 on unsafe/no-meta) and `GET /linkpreview/image?url=` → proxies `image/*` (≤2MB) so thumbnails satisfy `img-src 'self'`. Client `FB.linkpreview` (wwwroot/js/app.js) scans external `<a>` in `.note-card`, injects a `.link-preview` card (image/site/title/desc), and re-runs on live-timeline prepend + load-more via `FB.linkPreviewInit(root)`. CSS in site.css. 23 tests (parser, SSRF guard, caching, endpoint, JS/CSS presence).
 5. ✅ Content-warnings: blur + reveal, per-note and global; respected in lightbox. **Consistent blur** — `_NoteCard` now computes one `hasCw` flag (`Sensitive || ContentWarning`) and applies the `data-cw-content` + `note-attachment-blurred` treatment to the text, the image grid, *and* every video/audio media item the same way (previously text blurred only on sensitive-without-CW and images only on sensitive, so a CW-only note's media showed unblurred); CSS blurs `img`/`video`/`audio` under `.note-card.cw-hidden [data-cw-content]`. **Global preference** — `Get/SetBlurSensitiveMediaAsync` (EF + InMemory repos, `UserPreference` key `blur_sensitive_media`, default true) surfaced by a new `SettingsController` (`GET /settings` view with a "Blur sensitive media" toggle, `POST /settings/updatedisplay` upsert) + a Settings link in the Account nav. The timeline + card fragment resolve the viewer's pref and emit `data-cw-blur="true|false"` on the card root (CW note AND pref-on → true), so the initial blur is server-driven while the Show/Hide banner stays for manual reveal. **Re-runnable JS** — `FB.cwInit(root)` (wwwroot/js/app.js) applies the initial `cw-hidden` state from `data-cw-blur` and wires the Show/Hide toggle idempotently (`data-cw-bound`/`data-cw-applied` guards), and is re-invoked on live-timeline prepend, load-more, and like/boost reconcile (mirroring `FB.linkPreviewInit`). **Lightbox gate** — `FB.lightbox` detects the source card's `cw-hidden` state and opens in gated mode: the image renders `lightbox-img-blurred` under a `.lightbox-cw-gate` overlay; the Reveal button (or clicking the image) un-blurs *and* un-hides the source card's content, keeping the two in sync. Dark-theme banner readability + settings/lightbox-gate CSS added. 13 new tests (blur markup, plain-note gate, settings view/round-trip, card-fragment pref, `FB.cwInit` + wiring, lightbox gate, layout markup, CSS) — 1086/1086 passing.

### Phase 52: Real-World Federation Testing
Exercise the full stack against live remote instances and real users (integration host `https://openpub.luit.ink/`, primary target **@RayvenMX@mastodon.world**).
0. ✅ **Webfinger validation (remote→local) — local-side root cause fixed**: the "not found from remote" blocker was that actor federation URLs were hard-coded to `https://localhost/...` (unresolvable by remote instances). Fixed so all federation URLs derive from the request host: (a) `AuthController` stores the canonical actor id as `https://{host}/users/{username}` (only the host is dynamic; scheme pinned to `https`, path kept as the ActivityPub-standard `/users/{username}` the Core API + local follow logic expect), (b) Core `ActorController` (`/users/{username}` — the doc the webfinger `self` links to) normalizes id/inbox/outbox/followers/following/liked/publicKey from the request host, (c) WebUI `ActorsController` (`/actors/show`, `/actors/outbox|followers|following|liked`) derive URLs from the request host, (d) the duplicate WebUI webfinger controller was removed (the Core `/.well-known/webfinger` is authoritative and is auto-discovered by the WebUI's `MapControllers()`), (e) `FollowController.TryGetActorFromId` now matches the **current** host instead of hard-coding `https://localhost`. Chain verified: `GET /.well-known/webfinger?resource=acct:user@host` (Core, dynamic `Request.Host`) → `self` = `{host}/users/{user}` → Core `ActorController` serves the `Person` with request-host URLs. Covered by `WebFingerFederationTests` (6 tests). **Live cross-instance check still pending** (needs a reachable remote instance, e.g. mastodon.world): confirm the profile appears and renders like a standard Mastodon profile (avatar, display name, bio, follower/following counts, post list).
1. ⬜ **Locate real users**: follow **@RayvenMX@mastodon.world** from the Fediblog UI; verify the follow request/acceptance round-trip, the follower count updates on their profile, and the confirmation notification lands in our inbox.
2. ⬜ **Browse real remote profiles**: open @RayvenMX's profile and their posts via our UI; verify remote avatars, banners, note rendering, interaction counts, and pagination of their timeline.
3. ⬜ **Interact with a real user**: reply to one of their notes and like/boost it; verify the Activity is delivered (visible on their side / in their mentions), our optimistic UI reconciles with server truth, and notifications/badges update when they respond.
4. ⬜ **Inbound federation**: when the remote user interacts with us (likes, boosts, replies, follows back), verify we receive, store, deduplicate, and surface it (notifications, timeline, profile counts).
5. ⬜ **Federation health pass**: confirm outbound/inbound delivery latency, no duplicate or lost activities, graceful handling of remote errors/timeouts, and that remote content renders consistently across light/dark themes and at 320px.
6. ⬜ **Cleanup**: unfollow/unlike/unboost test interactions so we leave no lasting footprint on the remote user's account.

### Deferred (only if/when a UI feature requires it)
- OEmbed server-side, upload virus scan, size limits.
- Load testing with 100+ concurrent users (backend).

---

## Acceptance Criteria

- No page uses one-off styling inconsistent with the rest of the app.
- Dark mode toggles without flicker and persists; both themes pass WCAG AA.
- Every page is usable by keyboard alone and at 320px width.
- Timeline feels instant: new notes appear without full reload; pagination is client-driven.
- All changes verified by a delegated Playwright subagent with before/after screenshots.
