# Fediblog — UI & UX Plan

**Last Updated:** Aug 18, 2026
**Status:** Backend federation, API, and scalability infrastructure is feature-complete (Phases 1–45). UI phases 46–52 done except the Phase 52 live cross-instance checks (need the redeployed instance). Offline test suite is **1214/1214 green**; remaining Phase 52 items are the live cross-instance UI checks (deploy-blocked). New backend work is deferred until a UI feature requires it.

**WebUI:** `src/ActivityPub.WebUI/` — Razor MVC, SignalR, vanilla JS (`wwwroot/js/`), single `site.css` with `:root` design tokens + `[data-theme=dark]` overrides.

## UI Testing (brief)

- Browser/UI tests run in delegated Playwright subagents after each meaningful change batch, before marking a feature complete.
- Launch: `docker compose -f src/ActivityPub.WebUI/docker-compose.yml up -d --build`; wait for HTTP 200 at `http://localhost:8080`.
- Localhost has no real federation — mock remote rows in the DB (SQLite in `fediblog-data`) or POST crafted ActivityPub payloads to the local inbox.
- Integration host: `https://openpub.luit.ink/` (test target **@RayvenMX@mastodon.world**).
- Cleanup: `docker compose -f src/ActivityPub.WebUI/docker-compose.yml down` (add `-v` to discard DB fixtures).

## Remaining Work

### Phase 49: Design System & Visual Consistency
  - [x] Item 2 (done): tokenize spacing. **All iterations complete.** Iter 1–2: line-height + font-size + border-radius tokens. Iter 3: all **102 single-value** `padding`/`margin`/`gap` shorthands → the `--space-*` 0.25rem grid. Iter 4: all **138 multi-value** shorthands → the grid. Iter 5: all **101 longhand** `margin-(top|right|bottom|left)` / `padding-(top|right|bottom|left)` rhythm values → the grid (on-grid restated exactly, off-rhythm rounded; sub-4px pixel nudges like `margin-top:1px` kept raw as alignment, not rhythm). Permitted raw forms across all iterations: `0`/`auto` (reset/centering), negative lengths (`.sr-only` `margin:-1px` overlap + modal/hero negative positioning hacks), non-scale units (`vh`/`vw`/`em`/`%`), CSS functions (`var()`/`calc()`/`env()`, incl. `env(safe-area-inset-*)`), and custom properties (`--*`) + `/* … */` comment snippets. The `ComponentKitTests` spacing guard now scans **every** padding/margin/gap length token (shorthand + longhand, single + multi-value, paren-aware split so function calls stay whole) — **1137/1137 passing**. **Remaining (optional):** the pure `top/right/bottom/left` positioning offsets (centering `50%`/`100%`, sub-pixel nudges, negative modal hacks) are deliberately left raw — they are layout positioning, not rhythm spacing.
- [x] Item 4 (done): empty states, skeletons, and loading affordances on every data-bearing page. **Iter 1 (foundation, done):** shared design-system primitives in `site.css` + two reusable partials, de-duplicating the per-page markup. (a) Skeleton shimmer is now driven by theme tokens — `--skeleton-base`/`--skeleton-shine` (`:root` light `#e6e6ee`/`#f4f4f8`, `[data-theme=dark]` `#2a2a3c`/`#34344a`) — replacing the hard-coded `#e0e0e0`/`#f0f0f0` so it reads correctly in both themes. (b) New shared loading affordance: `.loading-indicator` + `.loading-spinner` + `@keyframes fb-spin`. (c) New shared partials: `Views/Shared/_SkeletonNoteCard.cshtml` (mirrors the real note-card layout; `@model bool` toggles the trailing media block) and `Views/Shared/_EmptyState.cshtml` (typed `EmptyStateModel`: Title/Hint/CtaLabel/CtaHref/CtaClass, `role="status"` + labelled heading for screen readers). (d) De-duplicated: `Timeline/Index.cshtml` + `Search/Index.cshtml` load-more skeletons now call the shared `_SkeletonNoteCard` (no more copy-pasted `.skeleton-avatar` markup); `Admin/Users.cshtml` + `Admin/Dashboard.cshtml` no-data tables render the shared `_EmptyState`; `MRF/Index.cshtml` lists show `.mrf-empty` hints. Added `@using ActivityPub.WebUI.Models` to `_ViewImports.cshtml`. 3 new `ComponentKitTests` (skeleton theme tokens + spinner, shared partials exist + markup, views use the shared partials) — **1140/1140 passing**. **Iter 2 (consolidation, done):** audited every remaining data-bearing view and routed **all 18** bespoke "no data" blocks onto the shared `_EmptyState` partial (accessible, labelled, one markup pattern). Converted: Notifications, Follow/Following (CTA), Follow/Followers, Hashtag, Trends, Communities/{Show,MyCommunities,Search,Index (CTA)}, Search (notes/users/hashtags ×3), Timeline, Admin/{Moderation (×3),AuditLog,Reports}, FederationHealth. Removed the 5 now-orphaned bespoke CSS classes (`.empty-timeline*` ×4, `.empty-notifications`, `.hashtag-empty`, `.no-data`, `.search-loading`). Standardized the Search "Searching…" indicator onto the shared `.loading-indicator`/`.loading-spinner` + `aria-busy` (JS now toggles `inline-flex`). The Search **prompt** (`.search-empty*`, a "start here" hero with an embedded link) is intentionally kept bespoke — it is not a data empty-state and its hint needs a live link. 3 new `ComponentKitTests` (no bespoke empty-state classes remain; all 18 pages route through `_EmptyState`; Search loading uses the shared spinner) — **1143/1143 passing**. **Iter 3 (load-more skeletons, done):** added fetch-time skeletons to the two feeds that were still missing them. (a) `Hashtag/Index.cshtml` — the note feed now carries `data-feed`/`data-card-selector=".note-card"`, each card a `data-activity-id`, and a standard `.load-more-container[data-next]` + `#load-more-skeleton` (shared `_SkeletonNoteCard`), so the existing `app.js` `loadmore` driver dedupes by activity id. (b) `Notifications/Index.cshtml` — the grouped list now carries `data-feed data-inject-selector=".notification-group"`, a standard load-more button, and a `#load-more-skeleton` of 3 new `_SkeletonNotificationRow` placeholders (mirrors the real `.notification-item` layout: round icon + title + muted line). `app.js` `loadmore` gained a second injection mode (`data-inject-selector`) for non-card feeds that clone every matched block (no per-card id to dedupe); note-card feeds are unchanged. New `.notifications-loading` CSS (stacks the rows; `.notification-skeleton` drops the card chrome). **Iter 4 (Profile empty-state, done):** the Profile page previously rendered only the header — no note list and no empty state when a user has no notes. Added the shared `_EmptyState` when `NoteCount == 0`: own profile shows "No notes yet" + a "Compose a note" CTA → `/Compose`; someone else's profile shows "No notes yet" + "<name> hasn't posted anything yet." (no CTA). Added `profile/index.cshtml` to the `DataBearingPages_RouteEmptyStateThroughSharedPartial` guard so it can't regress to a bespoke/missing empty state — **1157/1157 passing**. **Iter 5 (Playwright verification pass, done):** drove a freshly-registered user through a local instance (`127.0.0.1:9477`, isolated SQLite) and verified every empty/skeleton/loading affordance via computed-style extraction in light + dark + 320px. Confirmed: (a) Timeline empty-state — "No posts yet" + "Write your first post" CTA→`/Compose`; title `#1a1a2e`/`#e6e6f0` on `#f0f2f5`/`#12121a` (both >7:1 contrast). (b) **Profile empty-state (new, `f47e7f7`)** — "No notes yet" + "Compose a note" CTA→`/Compose`, renders correctly in dark. (c) Notifications "No notifications yet". (d) Shared `_SkeletonNoteCard` shimmer resolves to the theme tokens in both themes (light `#e6e6ee`, dark `#2a2a3c`). (e) After posting a note the empty-state is replaced by a real `.note-card` (avatar+content+actions). (f) 320px: no horizontal overflow on any page; every empty-state fits the viewport and stays centered. Only benign console noise was a 405 from the notifications widget's GET `markallread` auto-ping (POST-only endpoint). **Item 4 complete — all iterations done.**

### Phase 52: Real-World Federation Testing
Exercise the full stack against live remote instances (integration host `https://openpub.luit.ink/`, target **@RayvenMX@mastodon.world**). Webfinger local-side root cause is fixed (`WebFingerFederationTests`); the live cross-instance check below is still pending.
- [x] **Outbound resolver fixes (done):** real Mastodon actors ship `"@context"` as a JSON **array** (was `string?` → `Deserialize` threw and discarded the document) → `Actor.Context` is now a polymorphic `JsonNode?` via `JsonContextConverter`; WebFinger responses are served as `application/jrd+json` (was rejected by `ReadFromJsonAsync`) → body now parsed as string + `JsonNode.Parse`; outbound delivery hard-coded `{domain}/inbox` (404 on non-stock stacks) → now honors the resolved `inbox`/`sharedInbox` URL verbatim (SRV discovery only a last-resort fallback); webfinger self-link match relaxed (prefer `type=activity+json`, fall back to any `rel=self`) + `Accept` header added to webfinger + actor GETs. New `WebFingerServiceTests` (JRD + array-context regressions), 3 `OutboundActivityServiceTests` (inbox-honoring), and gated `LiveFederationTests` (`Category=LiveFederation`) round-trip vs mastodon.world — **1157/1157 passing**.
- [x] **Inbound "my user not found on remote" fix (done):** when searching `aluitink@openpub.luit.ink` from mastodon.world, the remote followed our webfinger `self` link which was `http://openpub.luit.ink/...` (the app ignored the TLS-terminating proxy, so `Request.Scheme`/`Host` were the internal `http://localhost`). nginx 301-redirects that to https, so the remote rejected the account. Fix: register the ASP.NET Core **Forwarded Headers** middleware (gated by `ActivityPub:ForwardedHeaders:Enabled`, off by default; `TrustedProxies` defaults to loopback) so `Request.Scheme`/`Host` honor `X-Forwarded-Proto`/`-Host`/`-For`. Verified on a live instance: with nginx headers the webfinger `self` link + actor `id`/`inbox`/`outbox`/`publicKey` now emit `https://openpub.luit.ink/...`; without headers behavior is unchanged (no regression). New `ForwardedHeadersOptions` in `ActivityPubOptions` + config in `appsettings.json`. **Deployment note:** the running instance must be redeployed with this build, and its nginx must send `X-Forwarded-Proto` (standard `proxy_set_header`).
- [x] **Outbound webfinger/actor-resolution verified live (done):** ran the gated `LiveFederation` tests against the real fediverse (egress available) — `ResolveActor_RealMastodonWorld_ReturnsActor` resolves @RayvenMX@mastodon.world into a full actor (preferred username, `id` containing mastodon.world, and an `inbox`/`sharedInbox` endpoint for outbound delivery) and `…_HandlesUnknownGracefully` returns `null` for a nonexistent handle. This confirms the Phase 52 outbound resolver fixes (array `@context` via `JsonContextConverter`, JRD body parse, inbox-honoring, relaxed self-link match + `Accept` header) work end-to-end against a real Mastodon instance — **2/2 passing**.
- [ ] Live cross-instance webfinger check (UI, needs deploy): confirm @RayvenMX's profile appears *from a remote instance* and renders like a standard Mastodon profile (avatar, display name, bio, follower/following counts, post list) in our UI.
- [ ] Locate real users: follow @RayvenMX from the Fediblog UI; verify follow request/acceptance round-trip, follower count updates, confirmation notification in our inbox.
- [ ] Browse real remote profiles: open their profile and posts via our UI; verify remote avatars, banners, note rendering, interaction counts, and timeline pagination.
- [ ] Interact with a real user: reply to and like/boost one of their notes; verify delivery on their side, optimistic UI reconciles with server truth, notifications/badges update on their response.
- [x] **Inbound receive+store verified live (done):** new gated `LiveFederation` test `Receive_RealMastodonWorldNote_ProcessesAndStores` — resolves @RayvenMX@mastodon.world, walks the real outbox `OrderedCollection → first → orderedItems[0]` (a real `Create` activity with a `Note` object), deserializes it through the app's `Activity` model, runs it through the real `InboxProcessor`, and asserts it is stored (`GetActivityAsync` returns it with matching `Type`/`ObjectId`). This confirms the *mechanism* behind the inbound item (we can receive + store a real remote activity) — **LiveFederation now 3/3**. The remaining "they interact with us" part (real-time follow/like/reply round-trip + notifications surfacing) still needs the deployed instance.
- [ ] Inbound federation (UI/live, needs deploy): when they interact with us (likes, boosts, replies, follow-back), verify we receive, store, dedupe, and surface it (notifications, timeline, profile counts).
- [x] **Graceful remote-error/timeout handling verified (done):** the federation graceful-failure contract (never throw on a misbehaving remote → degrade to `null` for webfinger / `false` for outbound, + log) was implemented but had ~zero offline coverage (only one 404-webfinger test). Added **9** tests: `WebFingerService` → 500, actor-fetch 500 (BadGateway), connection failure (`HttpRequestException`), timeout (`TaskCanceledException`), malformed non-JSON body; `OutboundActivityService` → 500, 404, connection failure, timeout. `StubHandler`/`CapturingHandler` gained an optional throw-factory to simulate resets/timeouts. This covers the **code-side half** of the federation-health item (graceful handling of remote errors/timeouts) — **1167/1167 passing**.
- [x] **No duplicate/lost activities verified (done, code-side):** the "no duplicate activities" federation-health guarantee was only covered by a *mocked*-repo unit test. Added `InboxDeadLetterTests.DuplicateInboundActivity_RealRepository_StoredExactlyOnce`, which drives the real inbound entry point `SharedInboxService.ProcessAndDistributeActivityAsync` **twice** with the identical activity against the real `InMemoryActivityPubRepository` and asserts both deliveries return `true` (duplicate is a no-op success, not an error → no loss/spurious rejection) and **exactly one** activity is stored + retrievable + marked seen. This is the code-side "no duplicate/lost activities" half of the federation-health item — **1168/1168 passing**.
- [x] **No lost activities verified (done, code-side):** the "no lost activities" guarantee for *dead-lettered* inbound activities was only covered by a *mocked*-repo test that proved the row is marked `Replayed` but never that the activity actually lands in the store. Added `InboxDeadLetterTests.DeadLetteredActivity_RealRepository_ReplayedAndStored_NoLostActivity`, driven entirely against the real `InMemoryActivityPubRepository`: seeds a `DeadLettered` DLQ row with its raw payload, asserts nothing is stored before replay, runs `ProcessInboxDeadLettersAsync`, and asserts `replayed == 1` + the row is `Replayed` (no failure reason) + the activity is now durably stored (single id, retrievable, marked seen). This is the code-side "no lost activities" half of the federation-health item — **1169/1169 passing**.
- [x] **FederationHealthService observability unit tests (done, code-side):** the federation health/observability surface (`FederationHealthService`) had only DI-registration checks and no unit tests of its logic. Added **8** offline unit tests in a new `FederationHealthServiceTests.cs`: `GetDeliveryQueueStatsAsync` (empty queue → all zeros + no divide-by-zero; a mix of all five `DeliveryStatus` values → correct Pending/Failed/MaxRetriesExceeded buckets, `OldestPending`, `LastSuccessfulDelivery`, and the `ErrorRate = (failed+maxed)/total*100` formula), `GetRecentErrorsAsync` (only Failed/MaxRetriesExceeded surface, most-recent-first, null reason → "Unknown", respects `limit`), and `GetHealthStatusAsync` → `DetermineOverallStatus` thresholds (no failures → "Healthy"; ErrorRate>20 → "Degraded"; ErrorRate>50 → "Critical"; PendingCount>1000 w/ 0% errors → "Warning"). Uses an empty InMemory `ActivityPubDbContext` + a mocked repository (all five status buckets are deterministic only via a mock, since the real InMemory repo filters out `MaxRetriesExceeded`/`Delivered`). This is the code-side "health observability" half of the federation-health item — **1177/1177 passing**.
- [x] **WebhookDeliveryService unit tests (done, code-side):** the webhook delivery path (`WebhookDeliveryService`) had **no** unit test (only a host-based DI check in `WebUI/ApiWebhookTests`). Added **10** offline tests in a new `WebhookDeliveryServiceTests.cs` using the real `InMemoryActivityPubRepository` (implements all webhook methods with real behavior) + a stub `HttpMessageHandler` for the HTTP leg: config upsert (create-then-update, no duplicate), `DeleteWebhookConfigAsync`, event-type-matched queueing (`All` matches every type; a type-mismatch and a disabled config queue nothing), the delivery loop (success → `Delivered` + a single history row + an `X-Webhook-Signature` HMAC-SHA256 header matching the (secret, body) pair; non-2xx → `Failed` + `RetryCount` 0→1 with the real status code; at `MaxRetries` → terminal `MaxRetriesExceeded`, no longer pending; missing config → `Failed` "Webhook configuration not found" with **no** HTTP call), and `VerifyWebhookSignatureAsync` (correct signature true; wrong signature / wrong secret / tampered body false). The new tests also surfaced and fixed **two real bugs** in the service: (1) `request.Headers.Add("Content-Type", ...)` threw `InvalidOperationException` ("misused header name") on every delivery — removed, since the `StringContent` already sends `application/json`; (2) `SaveDeliveryHistoryAsync` was called **twice** on success (once in `DeliverToWebhookAsync`, once in `ProcessPendingDeliveriesAsync`), double-writing a history row — now recorded once with the real status code/body. **1187/1187 passing.**
- [x] **Services ActivityPubEventDispatcher RemoveHandler bug fix (done, code-side):** there are two `ActivityPubEventDispatcher` classes. The one actually wired into the **runtime path** (`Core.Services.ActivityPubEventDispatcher`, registered in DI at `ActivityPubServiceCollectionExtensions:75` and used by `ActivityPubService.ProcessIncomingActivityAsync` + `InboxProcessorService.ProcessInboxItemsAsync`) had a real bug: `RemoveHandler` **added** the handler to its `ConcurrentBag` instead of removing it (the comment even said "simplified approach"), so a "removed" handler was still dispatched — and **twice** after an Add+Remove. Fixed by switching the store to a `ConcurrentDictionary` keyed by handler identity (thread-safe add + removal; re-add is idempotent) and snapshotting the key array in `DispatchAsync` (so a concurrent add/remove mid-dispatch can't throw a `ConcurrentDictionary` enumeration exception). Added **4** regression tests in `ActivityPubEventDispatcherTests` pinning the runtime dispatcher's Add/Remove/Dispatch contract (invoke on add; no invoke after remove; remove leaves other handlers intact; all handlers invoked). **1191/1191 passing.**
- [x] **DefaultWebFingerSource unit tests (done, code-side):** the `IWebFingerSource` implementation (`DefaultWebFingerSource`, the WebFinger resource resolver) had **no** unit test. Added **6** offline tests in a new `DefaultWebFingerSourceTests.cs` using a real `WebFingerCacheService` over an in-memory `MemoryCache` + a Moq `IActivityPubRepository` (the unused concrete deps — `ActivityPubService`, `ActivityPubTelemetry` — are constructed with null loggers / mock caches): `acct:` resolution returns the actor `Id` **and** populates the cache with a `self` link; a cache hit returns the cached href **without** a repository call (the repo lookup is wired to throw if invoked, proving the cache short-circuits); an unknown user returns `null`; a non-`acct:` resource returns `null`; an `acct:` resource without a `@domain` (e.g. `acct:alice`) returns `null`; and a second resolution of the same resource is served from cache (the repository is hit exactly once). **1197/1197 passing.**
- [x] **InMemoryApplicationRepository unit tests (done, code-side):** the OAuth client / authorization-code / access-token store (`InMemoryApplicationRepository`, the `IApplicationRepository` in-memory impl) had **no** direct unit test. Added **17** offline tests in a new `InMemoryApplicationRepositoryTests.cs`: client save/get round-trip; null/empty-client-id rejected; unknown/empty client-id lookup → null; `VerifyClientAsync` matches only the correct secret (unknown/empty rejected); `GetAllAsync` returns every client; `GetByOwnerAsync` filters by owner (unknown/empty → empty); authorization-code redemption (valid → returns + marks `IsUsed`; already-used → null; expired → null; single-use → second redeem null; null/empty code rejected); access-token round-trip; expired token → null; unknown/empty token → null; null/empty token rejected. **1214/1214 passing.**
- [ ] Federation health pass (live, needs deploy): outbound/inbound delivery latency, remote content renders consistently in light/dark and at 320px.
- [ ] Cleanup: unfollow/unlike/unboost test interactions so we leave no lasting footprint on the remote user's account.

### Deferred (only if/when a UI feature requires it)
- OEmbed server-side, upload virus scan, size limits.
- Load testing with 100+ concurrent users (backend).

## Acceptance Criteria

- No page uses one-off styling inconsistent with the rest of the app.
- Dark mode toggles without flicker and persists; both themes pass WCAG AA.
- Every page is usable by keyboard alone and at 320px width.
- Timeline feels instant: new notes appear without full reload; pagination is client-driven.
- All changes verified by a delegated Playwright subagent with before/after screenshots.
